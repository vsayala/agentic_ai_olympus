name: policy_mogul
kind: mcp
catalog: data
schema: policy_mogul
raw_volume: raw
checkpoint_volume: checkpoints
semantic_table: semantic_chunks
serving_endpoint: policy-mogul-hercules
options:
  mcp_url: ${POLICY_MOGUL_MCP_URL}
  connection: ${POLICY_MOGUL_CONNECTION}
  materialization: none
  governance: unity_ai_gateway